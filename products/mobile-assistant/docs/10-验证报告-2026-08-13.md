# 验证报告 · 2026-08-13 夜（自动化验证，用户睡觉期间执行）

> 本报告由 Claude Code 在用户授权范围内自动执行并记录。所有测试真实发生，成本已入账可查。

## 一、本轮完成的事

| # | 事项 | 结果 |
|---|------|------|
| 1 | 接入真实 DeepSeek key（复用 V2 配置，未打印密钥） | ✅ |
| 2 | 启用微信工具（用户授权读联系人；llm_visibility=aggregates，只送统计给 LLM） | ✅ |
| 3 | 安装 edge-tts（免费 TTS 兜底，MiniMax 未充值期间的正式方案） | ✅ |
| 4 | 下载 SenseVoice 模型（937MB，hf-mirror）并验证本地 STT | ✅ 4.6s 音频识别 180ms，文本正确 |
| 5 | 安装 sherpa-onnx 1.13.5（PyPI 直连抽风，改直接下载 wheel 安装） | ✅ |
| 6 | 编写完整 Flutter 客户端源码（`app/`，9 个 Dart 文件 + pubspec + 装配说明） | ✅ |
| 7 | 编写一键自检脚本 `scripts/e2e_check.py`（可随时复跑） | ✅ |
| 8 | 修复 2 个工程问题：TTS 队列哨兵丢失死锁（测试必现）、GBK 控制台 emoji 崩溃（3 个脚本） | ✅ |
| 9 | TTS 兜底格式如实上报（edge 输出 mp3/24k，不再谎报 pcm）；voice_chat.py 支持 mp3 播放（ffplay） | ✅ |
| 10 | 服务器改绑 0.0.0.0（家庭局域网供手机连接，token 鉴权） | ✅ |

## 二、端到端验证结果（e2e_check.py，6/6 通过）

```
PASS  健康检查: stt_engine=sensevoice, wechat_tools=True
PASS  文字对话(LLM): 事件35个增量 · 费用 ¥0.0019
PASS  微信联系人工具: 调用了 wechat_contact_list（真实读取微信库）
PASS  语音文件全链路: 识别='今天有什么重要的事情吗？' · TTS 222048 字节 · 费用 ¥0.0016
PASS  成本报表: 近7天 ¥0.0121 · 按服务商: {'deepseek': 0.0121, 'local': 0.0}
```

- **语音闭环**：edge-tts 生成语音 → 手机同款 WS 路径（100ms/帧）→ VAD 断句 → SenseVoice 识别（完全正确，含标点）→ DeepSeek 流式回答 → 句级 TTS（edge 兜底）→ 音频文件已保存 `server/data/e2e_tts_output.mp3`
- **微信工具**：`wechat_contact_list` 真实触发并读取成功（aggregates 档位，原始消息未送 LLM）
- **成本**：一整夜所有测试共花费 **约 ¥0.012**；本地 STT 记账为 `local: 0.0`（免费策略生效）

## 三、单元测试

`server/tests/`：**47/47 通过**（分块器/价格/工具/记忆/Persona/管线集成/VAD/TTS 队列/API/配置）

## 四、起床后你要做的 2 件事

1. **改 token**：编辑 `server/config/settings.yaml` → `server.token`（只在家庭网络内使用也建议设置强随机值）
2. **手机连 Miru**（三选一）：
   - 同一 Wi-Fi：App 设置页填 `http://<电脑局域网IP>:8765`（查 IP：`ipconfig`），token 同上
   - 或先在电脑上试：`cd products/mobile-assistant/server && .\.venv\Scripts\python scripts\voice_chat.py`（对麦克风说话，回车开始/结束）
   - Tailscale 方案见 `01-总体架构与选型.md §6`

## 五、已知限制（诚实声明）

- TTS 目前走 edge-tts 免费兜底（音色"晓晓"）；充值 MiniMax 后改 `settings.yaml → tts.minimax.api_key/group_id` 即可切换，代码零改动
- 语音链路用 wav 文件验证（模拟手机帧节奏）；真实麦克风 VAD 调参需真机/PC 麦克风实测
- Flutter 源码未编译验证（本机无 Flutter 环境），装配后如有小问题按 `app/README.md` 排查
- 服务日志在 `server/data/logs/`，重启方式：`cd products/mobile-assistant/server && .\.venv\Scripts\python -m miru_server`
