# Phase 2 portable cloud dependency set. No zeroconf, Daily Report, pymem,
# pysilk, sherpa-onnx, faster-whisper, numpy, CUDA, or local model package.
fastapi>=0.110
uvicorn[standard]>=0.29
websockets>=12
sqlalchemy>=2.0
openai>=1.40
httpx>=0.27
loguru>=0.7
pydantic>=2
pyyaml>=6
python-multipart>=0.0.9
Pillow>=10
edge-tts==7.2.8

# The Phase 2 image keeps current synchronous attachment compatibility. The
# isolated worker/parser limits remain a Phase 9 implementation task.
PyMuPDF>=1.24
openpyxl>=3.1
python-docx>=1.1
python-pptx>=1.0
